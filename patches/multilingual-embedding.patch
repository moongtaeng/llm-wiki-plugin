--- a/skills/llm-wiki/scripts/wiki_search.py
+++ b/skills/llm-wiki/scripts/wiki_search.py
@@ -53,7 +53,10 @@
 FRONTMATTER_RE = re.compile(r"^---\s*\n(.*?)\n---\s*\n", re.DOTALL)
 TOKEN_RE = re.compile(r"[a-z0-9]+")
 HEADING_RE = re.compile(r"^(#{1,6})\s+(.+?)\s*#*\s*$")
-LOCAL_EMBED_MODEL = "BAAI/bge-small-en-v1.5"
+# Fork-local: model is overridable via FASTEMBED_MODEL so non-English wikis
+# (e.g. Korean) can swap in a multilingual embedding model without editing
+# this file on every upstream update. See patches/multilingual-embedding.patch.
+LOCAL_EMBED_MODEL = os.environ.get("FASTEMBED_MODEL", "BAAI/bge-small-en-v1.5")
 VECTOR_INDEX_SCHEMA = "2"
 VECTOR_INDEX_NAME = "embeddings.sqlite"
 MAX_COSINE_DISTANCE = 0.35
